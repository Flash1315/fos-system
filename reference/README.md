# Reference: Ride & Joy Spending Bot

Full snapshot of `Flash1315/RJ-Spending-bot` for Fos agents — **read-only product/domain reference**.

```
reference/rj-spending-bot/
```

Start here inside the snapshot:
- `CONTEXT.md` — full ops/domain context
- `SUPERADMIN_GUIDE.md` / `INSTRUCTOR_GUIDE.md` — user-facing flows
- `.cursorrules` — RJ bot coding/notification rules (for understanding RJ only)
- `handlers/` — Telegram flows (expenses, fuel, income, rental, …)
- `utils/` — Sheets, calendar, GPS, AI, …

**Not included:** `credentials.json`, `.env`, runtime JSON state, venv.

**Do not run or deploy this copy as production.** Production bot stays in its own repo/server.
